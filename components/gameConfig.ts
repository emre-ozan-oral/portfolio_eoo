// Shared feature flag for the puzzle games' solver tooling: the
// auto-playing "Watch solver" demo tab and the in-game Hint button
// (Minesweeper only has the former). Flip this back to `true` to bring
// both back everywhere at once.
export const SOLVER_ENABLED = false;
